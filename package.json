{
  "name": "tabtrail",
  "version": "0.2.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "description": "TabTrail Chromium 浏览器插件，用于找回最近标签并整理当前打开的标签。",
  "scripts": {
    "test": "node --test tests",
    "check": "node scripts/check.js",
    "build": "node scripts/build.js",
    "verify": "npm test && npm run check"
  },
  "dependencies": {
    "driver.js": "1.3.6"
  }
}
