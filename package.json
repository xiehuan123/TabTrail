{
  "name": "tabtrail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TabTrail Chromium extension MVP for recent tab recovery and tab organization.",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check.js",
    "build": "node scripts/build.js",
    "verify": "npm test && npm run check"
  }
}
