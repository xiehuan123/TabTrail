## 1. 共享交互基础

- [x] 1.1 梳理 popup、side panel、newtab 当前状态文案、错误文案、空状态文案和列表 live region 使用情况。
- [x] 1.2 增加或整理共享状态公告约定，覆盖普通状态、错误状态、恢复提示和辅助技术公告。
- [x] 1.3 增加或整理焦点恢复 helper，支持用 `tabId` 和控件类型在重渲染后恢复合理焦点。
- [x] 1.4 定义统一的范围切换分段控件行为和样式，替换不完整的 tablist 语义。

## 2. Popup 交互重构

- [x] 2.1 为 popup 增加专用状态公告区域，覆盖侧边栏能力检测、打开结果、偏好保存结果和初始化失败。
- [x] 2.2 调整 popup 状态文案，使用户能区分“当前摘要”和“操作结果”。
- [x] 2.3 为 popup 最近活跃和最近关闭列表补充更明确的按钮可访问名称。
- [x] 2.4 更新 popup UI 测试，验证状态公告区域、错误状态和最近列表可访问名称。

## 3. Side Panel 交互重构

- [x] 3.1 将 side panel 范围切换改为分段按钮语义，并在摘要中展示当前范围和数量。
- [x] 3.2 移除标签列表整体 live region，改用专用状态公告节点公告搜索、归类、排序、关闭和错误结果。
- [x] 3.3 为固定、上移、下移等符号按钮增加包含动作和标签标题的 `aria-label`。
- [x] 3.4 为选择、上移、下移、归类和关闭后的列表重渲染增加焦点恢复。
- [x] 3.5 为排序预览增加未应用状态，只有排序变化后才启用或强调“应用排序”。
- [x] 3.6 为关闭已选标签增加范围和数量反馈，并提供撤销入口或明确最近关闭恢复路径。
- [x] 3.7 更新 side panel UI 和模型测试，覆盖范围语义、焦点恢复、排序状态、关闭恢复反馈和状态公告。

## 4. Newtab 交互重构

- [x] 4.1 将 newtab 范围切换改为分段按钮语义，并在摘要中展示当前范围和数量。
- [x] 4.2 修正 `<ul>` 内空状态语义，使用合法列表项或将空状态移出列表容器。
- [x] 4.3 移除分类按钮上误导性的 `aria-disabled`，改用可见说明表达是否可接收拖拽。
- [x] 4.4 为 newtab 增加专用状态公告区域，覆盖归类、拖拽、移动分类、应用排序、关闭分类和初始化失败。
- [x] 4.5 为单个标签增加不依赖拖拽的移动到分类路径，支持键盘完成。
- [x] 4.6 为 newtab 排序预览增加未应用状态、应用成功反馈和应用失败反馈。
- [x] 4.7 为关闭分类和关闭已选标签增加范围、数量和恢复反馈。
- [x] 4.8 更新 newtab UI 和模型测试，覆盖分类语义、空状态、非拖拽归类、排序状态、关闭恢复反馈和状态公告。

## 5. 高标签量和输入体验

- [x] 5.1 为 side panel 搜索增加轻量 debounce，减少每次输入触发完整查询和重渲染。
- [x] 5.2 为 newtab 搜索增加轻量 debounce，保持输入焦点稳定。
- [ ] 5.3 在 50 个以上标签的测试数据下手动验证搜索、选择、归类和排序没有明显卡顿或焦点丢失。

## 6. 验证和验收

- [x] 6.1 运行现有测试套件，修复与新交互契约相关的失败。
- [x] 6.2 增加 OpenSpec 对应能力的测试映射说明，确保 `interaction-state-feedback`、`keyboard-tab-organization`、`scope-aware-tab-actions` 都有测试覆盖。
- [ ] 6.3 进行纯键盘走查：popup 打开侧边栏、side panel 搜索/选择/排序/归类/关闭、newtab 分类/排序/关闭分类。
- [ ] 6.4 进行屏幕阅读器走查，确认状态公告不会朗读完整列表，并能读出范围、按钮动作和错误状态。
- [ ] 6.5 进行 200% 缩放和窄宽度检查，确认文本、按钮和状态反馈不重叠、不横向溢出。
