## 1. Chrome 发布约束确认

- [x] 1.1 记录 Chrome 官方发布流程调研结论，确认首次安装引导不新增权限、不远程加载代码、不误导新标签页搜索。
- [x] 1.2 梳理当前 manifest 权限与商店隐私披露文案，确认引导文案和发布描述一致。

## 2. 引导状态模型

- [x] 2.1 在偏好模型中增加 onboarding 状态默认值、归一化逻辑和保存逻辑。
- [x] 2.2 增加偏好测试，覆盖旧数据缺失 onboarding 字段、完成状态、跳过状态和 sync 写入失败。

## 3. 首次安装触发

- [x] 3.1 在 background 安装事件中区分首次安装和更新。
- [x] 3.2 首次安装时设置引导待展示状态，并尝试打开 TabTrail 新标签页工作台。
- [x] 3.3 打开新标签页失败时保留待展示状态，不抛出未处理异常。
- [x] 3.4 增加 background 测试，覆盖首次安装、更新、打开失败和无额外权限约束。

## 4. 新标签页引导界面

- [x] 4.1 在 newtab 中增加顶部首次安装引导面板，展示欢迎文案、三步核心路径和隐私/搜索边界说明。
- [x] 4.2 实现“开始使用 TabTrail”和“跳过”操作，保存状态并隐藏引导。
- [x] 4.3 保证引导展示时不阻塞搜索、范围切换、分类、最近活跃和最近关闭区域。
- [x] 4.4 增加“重新查看新手引导”低打扰入口。
- [x] 4.5 为引导面板补充响应式样式、焦点态、状态公告和可访问名称。

## 5. 文档和发布检查

- [x] 5.1 更新安装或发布说明，补充 Chrome Web Store 发布流程、隐私披露、权限检查和首次引导检查。
- [x] 5.2 更新 README 或隐私文档，说明首次安装引导不上传标签数据、不修改默认搜索引擎。
- [x] 5.3 增加发布前检查清单，确认没有远程脚本、没有新增无关权限、安装后只展示一次引导。

## 6. 验证

- [x] 6.1 更新 UI 测试，覆盖首次引导展示、完成、跳过、重新查看和不重复展示。
- [x] 6.2 运行 `npm test` 并修复失败。
- [x] 6.3 运行 `npm run check`，确认项目结构检查通过。
- [x] 6.4 运行 `npm run build`，确认扩展包能构建。
- [ ] 6.5 手动在 Chrome 加载 `dist/`，验证首次安装后打开引导页、更新不重复打开、完成/跳过后不再自动展示。
